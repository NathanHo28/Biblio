<% unless current_user?(@user) %>
	<div id="follow_form">
		<% if current_user.following?(@user) %>
			<%= link_to "Unfollow", unfollow_user_path(user_id: @user) %>
		<% else %>
			<%= link_to "Follow", follow_user_path(user_id: @user) %>
		<% end %>
	</div>
<% end %>